{
  "name": "familia-food-app",
  "version": "1.0.0",
  "private": true,
  "description": "Familia Food POS - Android build wrapper",
  "scripts": {
    "android:build": "npx cap sync android && cd android && ./gradlew assembleDebug"
  },
  "devDependencies": {
    "@capacitor/android": "^8.0.0",
    "@capacitor/cli": "^8.0.0",
    "@capacitor/core": "^8.0.0"
  }
}
